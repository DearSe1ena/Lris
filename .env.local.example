# ===== DeepSeek（OpenAI 兼容格式）接口配置 =====
# 复制本文件为 .env.local 并填入真实 Key（.env.local 已被 .gitignore 忽略，不会随分享泄露）

DEEPSEEK_API_KEY=sk-your-deepseek-api-key

# 默认即为 DeepSeek 官方接口根地址（AI SDK 会自动追加 /chat/completions，不要在这里带上它）；
# 可换成任意 OpenAI 兼容服务的根地址
DEEPSEEK_BASE_URL=https://api.deepseek.com
